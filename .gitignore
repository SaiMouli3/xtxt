/xtxt
*.html
!docs/*.html
!editors/**/*.html
.DS_Store
.claude/
__pycache__/
.pytest_cache/
node_modules/
*.egg-info/
sdk/python/dist/
sdk/js/*.tgz
sdk/rust/target/
sdk/java/target/
sdk/rust/Cargo.lock
sdk/c/build/
sdk/cpp/build/
editors/vscode/out/
editors/vscode/*.vsix

# Research working files. These live on a local branch and must never reach the
# public remote: the ignore rule has to be on main too, because a rule that only
# exists on a side branch does not apply once you switch back.
benchmark/
research/
*.vsix
